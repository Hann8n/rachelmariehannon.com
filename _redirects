# Cloudflare Pages redirects file
# /admin and /admin/ are served by Workers static assets as /admin/index.html by default.
# Removed /admin/* -> /admin/index.html 200 to avoid infinite redirect loop (code 10021).
