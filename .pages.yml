media: images

content:
  - name: site
    label: Site Content
    type: file
    path: content/site.json
    format: json
    fields:
      - { name: title, label: Site Title, type: string, required: true }
      - { name: subtitle, label: Subtitle, type: string, required: true }
      - { name: author, label: Author Name, type: string, required: true }
      
      - name: sections
        label: Page Sections
        type: object
        list: true
        fields:
          - name: type
            label: Section Type
            type: select
            required: true
            options:
              values:
                - text
                - quote
                - list
                - substack
                - two-column
          
          - { name: title, label: Title, type: string }
          - { name: tagline, label: Tagline, type: string }
          - { name: id, label: ID, type: string }
          
          # Text section
          - { name: content, label: Text Content, type: text }
          
          - name: paragraphs
            label: Additional Paragraphs
            type: object
            list: true
            fields:
              - { name: text, label: Text, type: text, required: true }
          
          # Quote section
          - { name: quote, label: Quote, type: text }
          - { name: attribution, label: Attribution, type: string }
          
          # List section
          - name: items
            label: Items
            type: object
            list: true
            fields:
              - { name: title, label: Title, type: string, required: true }
              - { name: description, label: Description, type: text, required: true }
          
          # Two-column section
          - name: left_column
            label: Left Column
            type: object
            fields:
              - { name: title, label: Title, type: string }
              - name: items
                label: Items
                type: object
                list: true
                fields:
                  - { name: title, label: Title, type: string, required: true }
                  - { name: description, label: Description, type: text, required: true }
          
          - name: right_column
            label: Right Column
            type: object
            fields:
              - { name: title, label: Title, type: string }
              - { name: content, label: Content, type: text }
          
          # Substack section
          - { name: description, label: Description, type: text }
          - { name: button_text, label: Button Text, type: string }
          - { name: button_url, label: Button URL, type: string }
          - { name: embed_id, label: Embed ID, type: string }
